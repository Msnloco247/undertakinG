---
import Layout from '../layouts/Layout.astro';
import CardFodaZona from '../components/CardFodaZona.astro';
import CardProductoEstrategia from '../components/CardProductoEstrategia.astro';
import CardPasosPresupuesto from '../components/CardPasosPresupuesto.astro';
import LocationAutocomplete from '../components/forms/LocationAutocomplete.astro';
import CaptchaGuard from '../components/forms/CaptchaGuard.astro';
---

<Layout
  title="EmprendeIA — Analiza tu Emprendimiento con IA"
  description="Responde algunas preguntas sobre tu idea de negocio y obtén un análisis completo con FODA, propuesta de valor y plan de pasos con presupuesto."
>
  <!-- Nav -->
  <nav class="nav">
    <div class="nav-inner">
      <span class="nav-logo">
        <span class="logo-cube">⬡</span>
        EmprendeIA
      </span>
      <span class="nav-badge">MVP · Beta</span>
    </div>
  </nav>

  <main class="main">
    <!-- ── Hero ─────────────────────────────────────────────────────────────── -->
    <section class="hero">
      <div class="hero-eyebrow">Análisis de Emprendimientos con IA</div>
      <h1 class="hero-title">
        Tu idea merece<br />
        <em>un diagnóstico real</em>
      </h1>
      <p class="hero-desc">
        Responde el cuestionario y la inteligencia artificial generará tu
        análisis FODA, propuesta de valor y plan de acción con presupuesto
        estimado — todo en segundos.
      </p>
    </section>

    <!-- ── Form ──────────────────────────────────────────────────────────────── -->
    <section class="form-section">
      <form id="emprendimiento-form" autocomplete="off" novalidate>
        <div class="form-questions">

          <!-- Q1: Ubicación con Autocomplete -->
          <LocationAutocomplete />

          <!-- Q2: Producto / Servicio -->
          <div class="q-group" data-field="producto">
            <label class="q-label" for="q-producto">
              <span class="q-num">02</span>
              ¿Cuál es el producto o servicio que vas a ofrecer?
              <span class="q-required">*</span>
            </label>
            <textarea
              id="q-producto"
              name="producto"
              class="q-input q-textarea"
              rows="2"
              placeholder="Ej: Desarrollo y venta de repostería saludable sin azúcar."
              maxlength="300"
              required
              aria-describedby="q-producto-error q-producto-counter"
            ></textarea>
            <span class="q-error hidden" id="q-producto-error" role="alert"></span>
            <span class="q-counter" id="q-producto-counter">0 / 300</span>
          </div>

          <!-- Q3: Necesidad / Problema -->
          <div class="q-group" data-field="necesidad">
            <label class="q-label" for="q-necesidad">
              <span class="q-num">03</span>
              ¿Qué necesidad va a satisfacer o problema logra resolver?
              <span class="q-required">*</span>
            </label>
            <textarea
              id="q-necesidad"
              name="necesidad"
              class="q-input q-textarea"
              rows="2"
              placeholder="Ej: Ofrecer opciones dulces y deliciosas para diabéticos."
              maxlength="300"
              required
              aria-describedby="q-necesidad-error q-necesidad-counter"
            ></textarea>
            <span class="q-error hidden" id="q-necesidad-error" role="alert"></span>
            <span class="q-counter" id="q-necesidad-counter">0 / 300</span>
          </div>

          <!-- Q4: Público objetivo -->
          <div class="q-group" data-field="publico">
            <label class="q-label" for="q-publico">
              <span class="q-num">04</span>
              ¿A quién va dirigido?
              <span class="q-required">*</span>
            </label>
            <input
              type="text"
              id="q-publico"
              name="publico"
              class="q-input"
              placeholder="Ej: A adultos mayores con problemas de salud y jóvenes fitness."
              maxlength="200"
              required
              aria-describedby="q-publico-error"
            />
            <span class="q-error hidden" id="q-publico-error" role="alert"></span>
          </div>

          <!-- Q5: Medios de pago -->
          <div class="q-group" data-field="pagos">
            <label class="q-label" for="q-pagos">
              <span class="q-num">05</span>
              Medios de pago de los clientes
              <span class="q-required">*</span>
            </label>
            <input
              type="text"
              id="q-pagos"
              name="pagos"
              class="q-input"
              placeholder="Ej: Transferencias en bolívares, PagoMóvil y efectivo en divisas."
              maxlength="200"
              required
              aria-describedby="q-pagos-error"
            />
            <span class="q-error hidden" id="q-pagos-error" role="alert"></span>
          </div>

          <!-- Q6: Contexto adicional -->
          <div class="q-group" data-field="contexto">
            <label class="q-label" for="q-contexto">
              <span class="q-num">06</span>
              ¿Hay algo más que debamos saber?
              <span class="q-optional">(opcional)</span>
            </label>
            <textarea
              id="q-contexto"
              name="contexto"
              class="q-input q-textarea"
              rows="3"
              placeholder="Ej: Abriremos un pequeño local físico en el este de la ciudad, pero..."
              maxlength="1000"
              aria-describedby="q-contexto-hint q-contexto-counter"
            ></textarea>
            <span class="q-hint" id="q-contexto-hint">
              Presupuesto, experiencia previa, limitaciones, canales de venta, etc.
            </span>
            <span class="q-counter" id="q-contexto-counter">0 / 1000</span>
          </div>
        </div>

        <!-- ── Anti-bot Checkbox ──────────────────────────────────────────── -->
        <CaptchaGuard />

        <!-- ── Submit ─────────────────────────────────────────────────────── -->
        <button type="submit" class="submit-btn" id="submit-btn" disabled>
          <span class="btn-text" id="btn-text">Verificando…</span>
          <span class="btn-loader hidden" id="btn-loader" aria-hidden="true">
            <svg class="spin" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" opacity="0.25"/>
              <path d="M22 12a10 10 0 0 0-10-10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
            </svg>
            Analizando…
          </span>
          <span class="btn-icon" aria-hidden="true">→</span>
        </button>
      </form>
    </section>

    <!-- ── Results ───────────────────────────────────────────────────────────── -->
    <section id="results-section" class="results-section hidden" aria-live="polite">
      <div class="results-header">
        <h2 class="results-title">Tu plan</h2>
        <p class="results-subtitle">Los 3 análisis se generaron en paralelo</p>
      </div>
      <div class="results-grid">
        <CardFodaZona />
        <CardProductoEstrategia />
        <CardPasosPresupuesto />
      </div>
    </section>
  </main>

  <footer class="footer">
    <p>Generado por <span class="footer-brand">API de Análisis</span> · Potenciado por IA</p>
  </footer>
</Layout>

<!-- ── Client-side script ──────────────────────────────────────────────────── -->
<script>
import type {
  PreguntasRequest,
  FodaZonaResponse,
  ProductoEstrategiaResponse,
  PasosPresupuestoResponse,
} from '../types/api';
import { ApiError } from '../types/api';
import {
  fetchFodaZona,
  fetchProductoEstrategia,
  fetchPasosPresupuesto,
} from '../services/apiService';

// ── Validation helpers ─────────────────────────────────────────────────────

const VALIDATORS: Record<string, (v: string) => string | null> = {
  ubicacion: (v) => {
    if (!v.trim()) return 'Indica la ciudad o región donde operarás.';
    if (v.trim().length < 3) return 'Ingresa una ubicación válida.';
    return null;
  },
  producto: (v) => {
    if (!v.trim()) return 'Describe el producto o servicio.';
    if (v.trim().length < 10) return 'Por favor, sé más descriptivo.';
    return null;
  },
  necesidad: (v) => {
    if (!v.trim()) return 'Explica qué necesidad o problema resuelve.';
    if (v.trim().length < 10) return 'Por favor, brinda más detalle sobre la necesidad.';
    return null;
  },
  publico: (v) => {
    if (!v.trim()) return 'Define a quién va dirigido tu producto.';
    if (v.trim().length < 5) return 'Sé más específico sobre tu público objetivo.';
    return null;
  },
  pagos: (v) => {
    if (!v.trim()) return 'Indica los medios de pago que aceptarás.';
    return null;
  },
  contexto: () => null, // Optional
};

function showError(fieldName: string, message: string): void {
  const group = document.querySelector<HTMLElement>(`[data-field="${fieldName}"]`);
  const errEl = document.getElementById(`q-${fieldName}-error`);
  const input = document.getElementById(`q-${fieldName}`);
  if (!group || !errEl || !input) return;
  errEl.textContent = message;
  errEl.classList.remove('hidden');
  group.dataset.state = 'error';
  input.setAttribute('aria-invalid', 'true');
}

function clearError(fieldName: string): void {
  const group = document.querySelector<HTMLElement>(`[data-field="${fieldName}"]`);
  const errEl = document.getElementById(`q-${fieldName}-error`);
  const input = document.getElementById(`q-${fieldName}`);
  if (!group || !errEl || !input) return;
  errEl.textContent = '';
  errEl.classList.add('hidden');
  delete group.dataset.state;
  input.setAttribute('aria-invalid', 'false');
}

function markValid(fieldName: string): void {
  const group = document.querySelector<HTMLElement>(`[data-field="${fieldName}"]`);
  if (!group) return;
  group.dataset.state = 'valid';
}

function validateField(name: string, value: string): boolean {
  const validator = VALIDATORS[name];
  if (!validator) return true;
  const err = validator(value);
  if (err) {
    showError(name, err);
    return false;
  }
  clearError(name);
  if (name !== 'contexto' || value.trim().length > 0) {
    markValid(name);
  }
  return true;
}

// ── Character counters ─────────────────────────────────────────────────────

function setupCounter(inputId: string, counterId: string, max: number): void {
  const input = document.getElementById(inputId) as HTMLTextAreaElement | null;
  const counter = document.getElementById(counterId);
  if (!input || !counter) return;
  input.addEventListener('input', () => {
    const len = input.value.length;
    counter.textContent = `${len} / ${max}`;
    counter.dataset.warn = len > max * 0.85 ? 'true' : 'false';
  });
}

setupCounter('q-producto', 'q-producto-counter', 300);
setupCounter('q-necesidad', 'q-necesidad-counter', 300);
setupCounter('q-contexto', 'q-contexto-counter', 1000);

// ── Blur validation ────────────────────────────────────────────────────────

const FIELDS = ['ubicacion', 'producto', 'necesidad', 'publico', 'pagos', 'contexto'] as const;

FIELDS.forEach((name) => {
  const el = document.getElementById(`q-${name}`);
  el?.addEventListener('blur', (e) => {
    const val = (e.target as HTMLInputElement | HTMLTextAreaElement).value;
    // Don't validate instantly on blur if empty and optional, else do
    if (name !== 'contexto') validateField(name, val);
  });
  el?.addEventListener('input', (e) => {
    const val = (e.target as HTMLInputElement | HTMLTextAreaElement).value;
    const group = document.querySelector<HTMLElement>(`[data-field="${name}"]`);
    if (group?.dataset.state === 'error') validateField(name, val);
  });
});

// ── Validation before submit ───────────────────────────────────────────────

// Listen to captcha events to enable the button
document.addEventListener('captcha-solved', () => {
  const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement | null;
  const btnText = document.getElementById('btn-text');
  if (submitBtn && btnText) {
    submitBtn.disabled = false;
    btnText.textContent = 'Analizar mi emprendimiento';
  }
});

function validateAll(): boolean {
  let allValid = true;
  for (const name of FIELDS) {
    const el = document.getElementById(`q-${name}`) as HTMLInputElement | HTMLTextAreaElement | null;
    const val = el?.value ?? '';
    if (!validateField(name, val)) allValid = false;
  }
  
  const captcha = document.getElementById('main-captcha') as any;
  if (!captcha || !captcha.isSolved) {
    captcha?.showError?.();
    const wrapper = captcha?.querySelector('.captcha-wrapper');
    if (wrapper) {
      wrapper.classList.add('shake');
      wrapper.addEventListener('animationend', () => wrapper.classList.remove('shake'), { once: true });
    }
    allValid = false;
  }
  
  return allValid;
}

// ── DOM helpers for cards ──────────────────────────────────────────────────

function showSkeleton(cardId: string): void {
  document.getElementById(`skeleton-${cardId}`)?.classList.remove('hidden');
  document.getElementById(`error-${cardId}`)?.classList.add('hidden');
  document.getElementById(`content-${cardId}`)?.classList.add('hidden');
}

function showCardError(cardId: string, msg: string): void {
  document.getElementById(`skeleton-${cardId}`)?.classList.add('hidden');
  document.getElementById(`content-${cardId}`)?.classList.add('hidden');
  const errEl = document.getElementById(`error-${cardId}`);
  const msgEl = document.getElementById(`error-${cardId}-msg`);
  if (errEl) errEl.classList.remove('hidden');
  if (msgEl) msgEl.textContent = msg;
}

function showCardContent(cardId: string): void {
  document.getElementById(`skeleton-${cardId}`)?.classList.add('hidden');
  document.getElementById(`error-${cardId}`)?.classList.add('hidden');
  document.getElementById(`content-${cardId}`)?.classList.remove('hidden');
}

function setText(id: string, text: string): void {
  const el = document.getElementById(id);
  if (el) el.textContent = text;
}

function setList(id: string, items: string[]): void {
  const el = document.getElementById(id);
  if (!el) return;
  el.innerHTML = items
    .map((item) => `<li>${escHtml(item)}</li>`)
    .join('');
}

function escHtml(s: string): string {
  if (!s) return '';
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

// ── Render functions ───────────────────────────────────────────────────────

function renderFodaZona(data: FodaZonaResponse): void {
  const { analisisFODA, analisisZona } = data.analisis;

  // FODA items
  const renderFodaList = (id: string, items: Array<{ keyword: string; descripcion: string }>) => {
    const el = document.getElementById(id);
    if (!el) return;
    el.innerHTML = items
      .map(
        (item) =>
          `<li><span class="foda-keyword">${escHtml(item.keyword)}:</span> <span class="foda-desc">${escHtml(item.descripcion)}</span></li>`,
      )
      .join('');
  };

  renderFodaList('foda-fortalezas', analisisFODA.fortalezas);
  renderFodaList('foda-oportunidades', analisisFODA.oportunidades);
  renderFodaList('foda-debilidades', analisisFODA.debilidades);
  renderFodaList('foda-amenazas', analisisFODA.amenazas);

  // Zone
  setText('zona-descripcion', analisisZona.descripcion);
  setList('zona-ventajas', analisisZona.ventajas);
  setList('zona-desventajas', analisisZona.desventajas);
  setText('zona-recomendacion', analisisZona.recomendaciones);

  setText('foda-modelo', data.modelo);
  showCardContent('foda');
}

function renderProductoEstrategia(data: ProductoEstrategiaResponse): void {
  const { analisisProducto, estrategiasClave } = data.analisis;

  setText('producto-propuesta', analisisProducto.propuestaValor);
  setText('producto-descripcion', analisisProducto.descripcion);
  setText('producto-publico', analisisProducto.publicoObjetivo);

  const difEl = document.getElementById('producto-diferenciadores');
  if (difEl) {
    difEl.innerHTML = analisisProducto.diferenciadores
      .map((d) => `<li>${escHtml(d)}</li>`)
      .join('');
  }

  setList('estrategia-corto', estrategiasClave.corto_plazo);
  setList('estrategia-mediano', estrategiasClave.mediano_plazo);
  setList('estrategia-largo', estrategiasClave.largo_plazo);

  setText('producto-modelo', data.modelo);
  showCardContent('producto');
}

function renderPasosPresupuesto(data: PasosPresupuestoResponse): void {
  const { pasos, rango_presupuesto_total } = data.analisis;

  setText('pasos-total', rango_presupuesto_total);
  setText('pasos-modelo', data.modelo);

  const listaEl = document.getElementById('pasos-lista');
  if (listaEl) {
    listaEl.innerHTML = pasos
      .map((paso, i) => {
        return `
        <li class="paso-item">
          <header class="paso-header-row">
            <span class="paso-number">PASO ${i + 1}</span>
            <div class="paso-phase">${escHtml(paso.fase)}</div>
            <h3 class="paso-nombre">${escHtml(paso.paso)}</h3>
            <div class="paso-divider" aria-hidden="true"></div>
          </header>

          <div class="paso-data-grid">
            <div class="data-row">
              <span class="data-key">Descripción detallada</span>
              <div class="data-value">${escHtml(paso.descripcionDetallada)}</div>
            </div>
            
            <div class="data-row highlight">
              <span class="data-key">Presupuesto Estimado</span>
              <div class="data-value">Costo: ${escHtml(paso.presupuestoEstimado)}</div>
            </div>
            
            <div class="data-row">
              <span class="data-key">Justificación</span>
              <div class="data-value">${escHtml(paso.justificacionCostos)}</div>
            </div>

            <div class="data-row">
              <span class="data-key">Contexto local y leyes</span>
              <div class="data-value">${escHtml(paso.contextoLocalYLeyes)}</div>
            </div>
          </div>

          <!-- Spacer between steps -->
          <div class="paso-spacer" aria-hidden="true"></div>
        </li>`;
      })
      .join('');
  }

  showCardContent('pasos');
}

// ── Form submit ────────────────────────────────────────────────────────────

const form = document.getElementById('emprendimiento-form') as HTMLFormElement;
const btnLoader = document.getElementById('btn-loader')!;
const btnText = document.getElementById('btn-text')!;
const btnIcon = document.querySelector<HTMLElement>('.btn-icon')!;
const resultsSection = document.getElementById('results-section')!;
const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;

form.addEventListener('submit', async (e) => {
  e.preventDefault();
  if (!validateAll()) return;

  // Build request
  const getValue = (id: string) =>
    (document.getElementById(`q-${id}`) as HTMLInputElement | HTMLTextAreaElement).value.trim();

  const ubicacion = getValue('ubicacion');
  const producto  = getValue('producto');
  const necesidad = getValue('necesidad');
  const publico   = getValue('publico');
  const pagos     = getValue('pagos');
  const contexto  = getValue('contexto');

  // Construcción EXACTA mapeada a como el backend la espera
  const respuestasCliente: string[] = [
    `¿Cuál es el producto o servicio que vas a ofrecer?: ${producto}`,
    `¿Qué necesidad va a satisfacer o problema logra resolver?: ${necesidad}`,
    `¿A quién va dirigido?: ${publico}`,
    `Medios de pago de los clientes: ${pagos}`
  ];

  const req: PreguntasRequest = {
    ubicacion: ubicacion,
    respuestasCliente: respuestasCliente,
    contexto: contexto || undefined,
  };

  // UI → loading state
  submitBtn.disabled = true;
  btnText.classList.add('hidden');
  btnLoader.classList.remove('hidden');
  if (btnIcon) btnIcon.classList.add('hidden');

  // Show results section with skeletons
  resultsSection.classList.remove('hidden');
  showSkeleton('foda');
  showSkeleton('producto');
  showSkeleton('pasos');

  resultsSection.scrollIntoView({ behavior: 'smooth', block: 'start' });

  // Fire 3 requests in parallel
  const [fodaResult, productoResult, pasosResult] = await Promise.allSettled([
    fetchFodaZona(req),
    fetchProductoEstrategia(req),
    fetchPasosPresupuesto(req),
  ]);

  if (fodaResult.status === 'fulfilled') {
    renderFodaZona(fodaResult.value);
  } else {
    const err = fodaResult.reason;
    showCardError('foda', err instanceof ApiError ? err.message : 'Error de conexión con el servidor.');
  }

  if (productoResult.status === 'fulfilled') {
    renderProductoEstrategia(productoResult.value);
  } else {
    const err = productoResult.reason;
    showCardError('producto', err instanceof ApiError ? err.message : 'Error de conexión con el servidor.');
  }

  if (pasosResult.status === 'fulfilled') {
    renderPasosPresupuesto(pasosResult.value);
  } else {
    const err = pasosResult.reason;
    showCardError('pasos', err instanceof ApiError ? err.message : 'Error de conexión con el servidor.');
  }

  // Restore button
  submitBtn.disabled = false;
  btnText.classList.remove('hidden');
  btnText.textContent = 'Analizar de nuevo';
  btnLoader.classList.add('hidden');
  if (btnIcon) btnIcon.classList.remove('hidden');
});
</script>

<style is:global>
  /* ── Nav ──────────────────────────────────────────────────────────────────── */
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: hsl(220 14% 7% / 0.85);
    backdrop-filter: blur(12px) saturate(1.4);
    border-bottom: 1px solid var(--border-subtle);
  }
  .nav-inner {
    max-width: 900px;
    margin: 0 auto;
    padding: var(--sp-4) var(--sp-6);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .nav-logo {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-primary);
    display: flex;
    align-items: center;
    gap: var(--sp-2);
  }
  .logo-cube {
    color: var(--amber);
    font-size: 1.1rem;
  }
  .nav-badge {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    background: var(--bg-raised);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-sm);
    padding: 2px var(--sp-2);
  }

  /* ── Main ─────────────────────────────────────────────────────────────────── */
  .main {
    max-width: 900px;
    margin: 0 auto;
    padding: var(--sp-12) var(--sp-6) var(--sp-20);
  }

  /* ── Hero ─────────────────────────────────────────────────────────────────── */
  .hero {
    text-align: center;
    margin-bottom: var(--sp-12);
    animation: fadeUp 0.7s var(--ease-out) both;
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .hero-eyebrow {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--amber);
    margin-bottom: var(--sp-4);
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(2.2rem, 6vw, 3.8rem);
    font-weight: 400;
    line-height: 1.1;
    color: var(--text-primary);
    margin-bottom: var(--sp-5);
  }
  .hero-title em {
    font-style: italic;
    color: hsl(42 90% 70%);
  }

  .hero-desc {
    max-width: 520px;
    margin: 0 auto;
    font-size: clamp(0.95rem, 2vw, 1.05rem);
    color: var(--text-secondary);
    line-height: 1.7;
  }

  /* ── Form section ─────────────────────────────────────────────────────────── */
  .form-section {
    animation: fadeUp 0.7s 0.1s var(--ease-out) both;
  }

  /* ── Question groups ──────────────────────────────────────────────────────── */
  .form-questions {
    display: flex;
    flex-direction: column;
    gap: var(--sp-6);
    margin-bottom: var(--sp-8);
  }

  .q-group {
    position: relative;
  }

  .q-label {
    display: flex;
    align-items: baseline;
    gap: var(--sp-2);
    font-size: 0.925rem;
    font-weight: 500;
    color: var(--text-primary);
    margin-bottom: var(--sp-2);
    line-height: 1.4;
    cursor: pointer;
  }

  .q-num {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    color: var(--amber);
    letter-spacing: 0.05em;
    flex-shrink: 0;
  }

  .q-required { color: hsl(0 65% 60%); font-size: 0.8rem; }
  .q-optional {
    font-size: 0.72rem;
    color: var(--text-muted);
    font-weight: 400;
  }

  /* Input styles */
  .q-input {
    width: 100%;
    background: var(--bg-control);
    border: 1px solid var(--border-default);
    border-radius: var(--radius-md);
    padding: var(--sp-3) var(--sp-4);
    font-family: var(--font-body);
    font-size: 0.9rem;
    color: var(--text-primary);
    transition: border-color 0.2s var(--ease-out), box-shadow 0.2s var(--ease-out);
    outline: none;
    resize: vertical;
  }
  .q-input::placeholder { color: var(--text-muted); }
  .q-input:focus {
    border-color: var(--border-focus);
    box-shadow: 0 0 0 3px var(--amber-glow);
  }
  .q-textarea { min-height: 80px; }

  /* Validation states */
  .q-group[data-state="error"] .q-input {
    border-color: hsl(0 65% 42%);
    box-shadow: 0 0 0 3px hsl(0 50% 20% / 0.4);
  }
  .q-group[data-state="valid"] .q-input {
    border-color: hsl(150 50% 35%);
  }

  .q-hint {
    display: block;
    margin-top: var(--sp-1);
    font-size: 0.78rem;
    color: var(--text-tertiary);
  }

  .q-error {
    display: block;
    margin-top: var(--sp-1);
    font-size: 0.8rem;
    color: hsl(0 70% 62%);
    font-weight: 500;
  }

  .q-counter {
    display: block;
    text-align: right;
    margin-top: var(--sp-1);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    color: var(--text-muted);
    transition: color 0.2s;
  }
  .q-counter[data-warn="true"] { color: var(--warning); }

  .hidden { display: none !important; }
  
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    20%       { transform: translateX(-4px); }
    40%       { transform: translateX(4px); }
    60%       { transform: translateX(-4px); }
    80%       { transform: translateX(2px); }
  }
  .shake { animation: shake 0.35s var(--ease-out); }

  /* ── Submit button ─────────────────────────────────────────────────────────── */
  .submit-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--sp-3);
    width: 100%;
    max-width: 340px;
    padding: var(--sp-4) var(--sp-8);
    background: var(--amber);
    color: var(--text-inverse);
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 600;
    border: none;
    border-radius: var(--radius-md);
    cursor: pointer;
    transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
    position: relative;
    overflow: hidden;
  }

  .submit-btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(255 255 255 / 0);
    transition: background 0.2s;
  }

  .submit-btn:hover:not(:disabled) {
    background: hsl(42 95% 62%);
    transform: translateY(-1px);
    box-shadow: 0 6px 20px hsl(42 80% 30% / 0.4);
  }

  .submit-btn:active:not(:disabled) {
    transform: translateY(0);
    box-shadow: none;
  }

  .submit-btn:disabled {
    opacity: 0.45;
    cursor: not-allowed;
    transform: none;
  }

  .btn-loader {
    display: flex;
    align-items: center;
    gap: var(--sp-2);
    font-size: 0.9rem;
  }

  .btn-icon {
    font-size: 1rem;
    transition: transform 0.2s;
  }
  .submit-btn:hover .btn-icon { transform: translateX(3px); }

  /* ── Results ──────────────────────────────────────────────────────────────── */
  .results-section {
    margin-top: var(--sp-16);
    animation: fadeUp 0.5s var(--ease-out) both;
  }

  .results-header {
    margin-bottom: var(--sp-8);
  }

  .results-title {
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 4vw, 2.4rem);
    font-weight: 400;
    margin-bottom: var(--sp-1);
  }

  .results-subtitle {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    letter-spacing: 0.05em;
  }

  .results-grid {
    display: flex;
    flex-direction: column;
    gap: var(--sp-6);
  }

  /* ── Footer ───────────────────────────────────────────────────────────────── */
  .footer {
    text-align: center;
    padding: var(--sp-8) var(--sp-6) var(--sp-10);
    border-top: 1px solid var(--border-subtle);
    font-size: 0.8rem;
    color: var(--text-muted);
  }
  .footer-brand { color: var(--text-tertiary); }

  @media (max-width: 600px) {
    .main { padding: var(--sp-8) var(--sp-4) var(--sp-12); }
    .captcha-box { max-width: 100%; }
    .submit-btn { max-width: 100%; }
  }
</style>
